import React from "react";

export const Music = (props: any) => {
    return (
         <div style={{display: "flex", justifyContent: 'center', marginTop: '100px'}}>
            <h1 style={{color: "black"}}>The only truth is music </h1>
        </div>
    )
}

